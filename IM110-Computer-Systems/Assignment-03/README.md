# IM110 Homework 3

```bash
sudo apt update
sudo apt install linux-source

cd /usr/src/linux-source-6.8.0
sudo tar -xjf linux-source-6.8.0.tar.bz2

sudo apt install g++-9
```

Check for correct version. You might have to change kKernelSourceCodeDirectory in trace-system-calls.cc