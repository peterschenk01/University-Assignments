# assignment04
To render `rec04.qmd` run: `quarto render rec04.qmd`.

```bash
sudo apt-get update
sudo apt-get install bison
sudo apt-get install libreadline-dev
sudo apt-get install flex libfl-dev
```